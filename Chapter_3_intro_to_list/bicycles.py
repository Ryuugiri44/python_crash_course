bicycles = ['trek','cannondale','redline','specalized']
print(bicycles)

# How to access an element in a list (we will use the index)

print(bicycles[0])

# using a string method in it

print(bicycles[0].title())

# returning the last one

print(bicycles[-1])
